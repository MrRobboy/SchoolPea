/**
 * ESGI : Cours de Langage C de Kevin TRANCHO.
 * Code d'illustration de concept.
 */

#include <stdio.h>
#include <stdlib.h>

int main() {
	float resultat = 1 / 2;
	printf("1 / 2 = %g\n", resultat);
	printf("1. / 2 = %g\n", 1. / 2);
	printf("1 / 2. = %g\n", 1 / 2.);
	printf("1. / 2. = %g\n", 1. / 2.);
	exit(EXIT_SUCCESS);
}