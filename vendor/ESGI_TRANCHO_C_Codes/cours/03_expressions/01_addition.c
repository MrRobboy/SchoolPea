/**
 * ESGI : Cours de Langage C de Kevin TRANCHO.
 * Code d'illustration de concept.
 */

#include <stdio.h>
#include <stdlib.h>

int main() {
	int first = 2;
	int second = 3;

	printf("%d + %d = %d\n",
		    first, second, first + second);
	exit(EXIT_SUCCESS);
}