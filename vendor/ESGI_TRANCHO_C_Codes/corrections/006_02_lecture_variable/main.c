/**
 * ESGI : Cours de Langage C de Kevin TRANCHO.
 * Correction de l'exercice 6.
 */

#include <stdio.h>
#include <stdlib.h>

int main() {
  
  double reel;
  
  printf("Entrez un réel : ");  
  scanf("%lf", &reel);
  printf("Voici votre réel : %g\n", reel);
  
  exit(EXIT_SUCCESS);
}